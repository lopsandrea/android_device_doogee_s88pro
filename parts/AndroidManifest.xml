<?xml version="1.0" encoding="utf-8"?>
<!--
     Copyright (C) 2026 The LineageOS Project

     SPDX-License-Identifier: Apache-2.0
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.lineageos.settings.doogee"
    android:sharedUserId="android.uid.system">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Serve a togglePanel(): uno dei gesti del sensore di impronte apre e
         chiude il pannello delle notifiche. È di livello "normal", quindi non
         va aggiunta anche a privapp-permissions-s88pro.xml. -->
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />

    <application
        android:label="@string/app_name"
        android:persistent="true">

        <receiver
            android:name=".BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".led.LedService"
            android:exported="false" />

        <service
            android:name=".led.LedNotificationListener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!--
             La voce nelle Impostazioni.

             L'icona non è un vezzo: senza il meta-data com.android.settings.icon
             la voce non viene mostrata affatto, pur essendo registrata.
             La categoria "homepage" la mette nella pagina principale, dove si
             trova senza doverla cercare in un sottomenù.
        -->
        <activity
            android:name=".DeviceSettingsActivity"
            android:label="@string/app_name"
            android:icon="@drawable/ic_settings_doogee"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.settings.action.IA_SETTINGS" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="com.android.settings.category"
                android:value="com.android.settings.category.ia.homepage" />
            <meta-data
                android:name="com.android.settings.icon"
                android:resource="@drawable/ic_settings_doogee" />
            <meta-data
                android:name="com.android.settings.icon_tintable"
                android:value="true" />
            <meta-data
                android:name="com.android.settings.summary"
                android:resource="@string/app_summary" />
        </activity>
    </application>
</manifest>
