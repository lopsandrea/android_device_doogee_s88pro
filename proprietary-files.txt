# MediaTek proprietary blobs extracted from the stock system.img.
# These are the components the GSI was missing: mtkfusionrild ran but kept
# looping on the rild-oem socket with no framework counterpart.
# Generated from the files actually present in the stock image.

# Telephony and IMS framework
system/framework/mediatek-common.jar
system/framework/mediatek-framework-net.jar
system/framework/mediatek-framework.jar
system/framework/mediatek-ims-base.jar
system/framework/mediatek-ims-common.jar
system/framework/mediatek-ims-extension-plugin.jar
system/framework/mediatek-ims-legacy.jar
system/framework/mediatek-services.jar
system/framework/mediatek-telecom-common.jar
system/framework/mediatek-telephony-base.jar
system/framework/mediatek-telephony-common.jar
system/framework/mediatek-wfo-legacy.jar
system/framework/mediatek-res.apk

# Permissions
system/etc/permissions/com.mediatek.wfo.legacy.xml
system/etc/permissions/privapp-permissions-mediatek.xml

# Native libraries
system/lib64/libmediatek_exceptionlog.so
system/lib64/libmtk_vt_wrapper.so
system/lib64/libmtkperf_client.so

# IMS (VoLTE) -- see docs/bringup/volte-stato-esperimento.md
#
# Libraries that the stock ROM also kept in /system/lib64 and that we lack:
# some live only in /vendor (and the system namespace cannot load them from
# there), others AOSP has removed outright -- libhwbinder and libhidltransport
# were folded into libhidlbase in Android 11.
#
# Without these, libmtk_vt_wrapper.so does not load and ImsService dies with
# UnsatisfiedLinkError as soon as it touches the video call provider.
system/lib64/vendor.mediatek.hardware.videotelephony@1.0.so
system/lib64/libvcodec_cap.so
system/lib64/libvcodec_capenc.so

# libhidltransport and libhwbinder must NOT be taken from stock: Android 12
# still builds them (deprecated, folded into libhidlbase) and copying them here
# would define them twice --
#   error: overriding commands for target .../libhidltransport.so
# They are added to PRODUCT_PACKAGES in device.mk instead, so the system
# versions are used, consistent with the rest of the build.

# com.android.ims.ImsServiceBase, which AOSP removed after Android 10 and which
# ImsService.apk still requires. It is installed under a different name so as
# not to clash with the ims-common.jar we build ourselves, which is needed.
system/framework/ims-common.jar:system/framework/mtk-ims-compat.jar

# FM radio.
#
# The LineageOS FMRadio app and the stock one are the same app: the library
# registers its native methods for exactly com/android/fmradio/FmNative, so the
# two fit together without adaptation. The driver is already running
# (fmradio_drv.ko loaded, /dev/fm present).
#
# In the stock ROM the library lives in product/lib*, which for us does not
# exist as a separate partition: it is installed into system. The product
# partition is not mounted under /mnt/stock, but extract-files.sh reads it from
# the image with debugfs, which needs no privileges.
product/lib/libfmjni.so:system/lib/libfmjni.so
product/lib64/libfmjni.so:system/lib64/libfmjni.so

# --------------------------------------------------------------------------
# THE VENDOR PARTITION
#
# Until September there were only the twelve MediaTek framework .jars here: the
# vendor.img the build produced weighed 24 MB against the 378 of the real one,
# and that was not a problem only because we never installed it -- the phone
# has always run on the stock vendor.
#
# It became one when we built the installable zip: that carries every partition
# inside, it wrote that image, and the phone was left without the MediaTek HALs
# and without the 826 KB vendor_sepolicy.cil. Bootloop.
#
# These are the 1622 files extracted from the stock vendor (the 12 symlinks
# live in Android.mk, because a file list cannot express them).
# --------------------------------------------------------------------------

# Vendor executables and services (110)

# 64-bit libraries (551)

# 32-bit libraries (405)

# Configuration: init, audio, sensors, vendor SELinux policy (484)

# Chip firmware (38)

# Vendor applications (12)

# Resource overlays (2)

# Resources (6)

# odm content, which here lives inside vendor (9)

# The rest
#   vendor/default.prop   (no: see device.mk, the useful properties are declared there)

# --------------------------------------------------------------------------
# NOT THESE: the build already produces them
#
# Seventy-two files in the stock vendor are HALs that AOSP compiles from
# source -- fingerprint, drm, gatekeeper, memtrack, vibrator, graphics -- and
# declaring them here would copy them over, with the error
#   error: overriding commands for target .../vendor/bin/hw/android.hardware...
#
# On those the build wins: its own are compiled against Android 13, the stock
# ones stayed at Android 10. They are listed below, commented out, so that we
# know they exist and that the choice was deliberate.
# --------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# THE VENDOR PARTITION IS NO LONGER HERE
#
# For a while the 1622 files extracted from the stock vendor were listed here,
# one by one. It worked for building the image, not for booting it: the build
# installs its own modules into /vendor anyway -- the HALs AOSP compiles -- and
# every file where the two overlap has to be decided by hand. We decided
# eighty-three of them before realising that the "newest wins" criterion is
# wrong for anything touching the SoC, and that every wrong choice shows up as
# a phone stuck on the logo, with no error to read.
#
# Vendor is now the stock image, taken as it is:
#   BOARD_PREBUILT_VENDORIMAGE := vendor/doogee/s88pro/vendor.img
# The build does not construct it, it copies it. It is bit-for-bit identical to
# the one that has always run on the phone, which removes an entire category of
# questions -- which HALs, which libraries, which versions -- that did not need
# answering.
#
# Only the system files stay here: the MediaTek framework .jars, without which
# mtkfusionrild keeps looping on the rild-oem socket.
# ---------------------------------------------------------------------------
