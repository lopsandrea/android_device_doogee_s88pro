# MediaTek proprietary blobs extracted from the stock system.img.
# These are the components the GSI was missing: mtkfusionrild ran but kept
# looping on the rild-oem socket with no framework counterpart.
# Generated from the files actually present in the stock image.

# Telephony and IMS framework
system/framework/mediatek-common.jar
system/framework/mediatek-framework-net.jar
system/framework/mediatek-framework.jar
system/framework/mediatek-ims-base.jar
system/framework/mediatek-ims-common.jar
system/framework/mediatek-ims-extension-plugin.jar
system/framework/mediatek-ims-legacy.jar
system/framework/mediatek-services.jar
system/framework/mediatek-telecom-common.jar
system/framework/mediatek-telephony-base.jar
system/framework/mediatek-telephony-common.jar
system/framework/mediatek-wfo-legacy.jar
system/framework/mediatek-res.apk:system/app/mediatek-res/mediatek-res.apk;PRESIGNED

# Permissions
system/etc/permissions/com.mediatek.wfo.legacy.xml
system/etc/permissions/privapp-permissions-mediatek.xml

# Native libraries.
#
# DISABLE_DEPS on every one of them. extract-utils reads the ELF's DT_NEEDED
# and writes a shared_libs list, which soong then insists on resolving to
# modules:
#
#   error: vendor/doogee/s88pro/Android.bp:42:1: "libmtkperf_client" depends
#     on undefined module "vendor.mediatek.hardware.mtkpower@1.0".
#
# Those dependencies are MediaTek HIDL interfaces that exist only as libraries
# inside the stock vendor image, which this device ships whole rather than
# building. There is no module to point at, and none is needed: the linker
# finds them at runtime through the vendor namespace.
system/lib64/libmediatek_exceptionlog.so;DISABLE_DEPS
system/lib64/libmtk_vt_wrapper.so;DISABLE_DEPS
system/lib64/libmtkperf_client.so;DISABLE_DEPS

# IMS (VoLTE) -- see docs/bringup/volte-stato-esperimento.md
#
# Libraries that the stock ROM also kept in /system/lib64 and that we lack:
# some live only in /vendor (and the system namespace cannot load them from
# there), others AOSP has removed outright -- libhwbinder and libhidltransport
# were folded into libhidlbase in Android 11.
#
# Without these, libmtk_vt_wrapper.so does not load and ImsService dies with
# UnsatisfiedLinkError as soon as it touches the video call provider.
system/lib64/vendor.mediatek.hardware.videotelephony@1.0.so;DISABLE_DEPS
system/lib64/libvcodec_cap.so;DISABLE_DEPS
system/lib64/libvcodec_capenc.so;DISABLE_DEPS

# libhidltransport and libhwbinder must NOT be taken from stock: Android 12
# still builds them (deprecated, folded into libhidlbase) and copying them here
# would define them twice --
#   error: overriding commands for target .../libhidltransport.so
# They are added to PRODUCT_PACKAGES in device.mk instead, so the system
# versions are used, consistent with the rest of the build.

# com.android.ims.ImsServiceBase, which AOSP removed after Android 10 and which
# ImsService.apk still requires. It is installed under a different name so as
# not to clash with the ims-common.jar we build ourselves, which is needed.
system/framework/ims-common.jar:system/framework/mtk-ims-compat.jar

# FM radio.
#
# libfmjni is NOT extracted, and that is deliberate.
#
# Up to LineageOS 21 it was: the stock library registers its native methods for
# exactly com/android/fmradio/FmNative, so the MediaTek fork and the LineageOS
# app fitted together without adaptation.
#
# LineageOS 22 builds the library itself. packages/apps/FMRadio commit 168f373
# ("jni: Move customisation to properties") took the config values MediaTek had
# hardcoded into its fork and moved them to ro.fm.* properties, which made the
# library device independent; commit f3eb07a dropped the soong_namespace that
# used to keep the source module out of the way. Shipping the blob now collides
# with the source module, and the build stops with
#
#   TARGET module FMRadio requires non-existent TARGET module: prebuilt_libfmjni:64
#
# because prefer:true renames the prebuilt to prebuilt_libfmjni while soong
# still exports it as libfmjni.
#
# The config values that were inside the blob are in device.mk, read out of it
# rather than guessed. See the comment there.

# ---------------------------------------------------------------------------
# THE VENDOR PARTITION IS NOT HERE, AND THAT IS DELIBERATE
#
# For a while the 1622 files of the stock vendor were listed below, one by one,
# so that the build could construct the image. It does not any more: the stock
# image ships whole and untouched,
#
#   BOARD_PREBUILT_VENDORIMAGE := vendor/doogee/s88pro/vendor.img
#
# which is what the charter asks -- a maintainer must not require a *modified*
# prebuilt vendor image, and a non-A/B device may rely on an OEM one as long
# as it asserts its version at flash time, which releasetools.py does.
#
# Constructing it is not a choice we postponed, it is one that cannot be made
# in this tree, and the reason is measured: a vendor built against the blobs'
# VNDK 29 needs BOARD_VNDK_VERSION := 29, and soong refuses that with
#   error: bionic/libc/Android.bp: "libc" depends on undefined module
#          "vendor_snapshot"
# A vendor snapshot is generated only from a complete Android 10 tree with the
# sources of the vendor modules. Those sources do not exist -- it is the same
# wall this whole project was born against, the one that led to rebuilding the
# kernel from the factory binary.
#
# What the blobs would have cost, measured and written down in case the
# question ever comes back: 85 files overlap between the blobs and what the
# build installs, which is 42 decisions and not 85, and 1537 of the 1622 need
# no decision at all. The old list survives in git -- `git show
# 9ef0088:proprietary-files.txt` -- with its 84 exclusions already made.
#
# Only the system files stay here: the MediaTek framework .jars, without which
# mtkfusionrild keeps looping on the rild-oem socket, and the libraries that
# go with them.
# ---------------------------------------------------------------------------
