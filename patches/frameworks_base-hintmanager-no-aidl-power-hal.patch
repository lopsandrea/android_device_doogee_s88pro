diff --git a/services/core/java/com/android/server/power/hint/HintManagerService.java b/services/core/java/com/android/server/power/hint/HintManagerService.java
index 9174855b6466e..5eb211d41d570 100644
--- a/services/core/java/com/android/server/power/hint/HintManagerService.java
+++ b/services/core/java/com/android/server/power/hint/HintManagerService.java
@@ -327,12 +327,10 @@ public final class HintManagerService extends SystemService {
         mPowerHalVersion = 0;
         mVendorApiLevel = Integer.parseInt(SystemProperties.get(PROPERTY_VENDOR_API_LEVEL, ""));
         mUsesFmq = false;
-        if (mPowerHal != null) {
-            try {
-                mSupportInfo = getSupportInfo();
-            } catch (RemoteException e) {
-                throw new IllegalStateException("Could not contact PowerHAL!", e);
-            }
+        try {
+            mSupportInfo = getSupportInfo();
+        } catch (RemoteException e) {
+            throw new IllegalStateException("Could not contact PowerHAL!", e);
         }
         if (mSupportInfo.headroom.isCpuSupported) {
             mCpuHeadroomCache = new HeadroomCache<>(2, mSupportInfo.headroom.cpuMinIntervalMillis);
@@ -366,9 +364,14 @@ public final class HintManagerService extends SystemService {
 
     SupportInfo getSupportInfo() throws RemoteException {
         try {
-            mPowerHalVersion = mPowerHal.getInterfaceVersion();
-            if (mPowerHalVersion >= 6) {
-                return mPowerHal.getSupportInfo();
+            // A device whose vendor predates the AIDL power HAL has no
+            // mPowerHal at all. Fall through to the defaults below rather than
+            // leaving mSupportInfo null: the constructor dereferences it.
+            if (mPowerHal != null) {
+                mPowerHalVersion = mPowerHal.getInterfaceVersion();
+                if (mPowerHalVersion >= 6) {
+                    return mPowerHal.getSupportInfo();
+                }
             }
         } catch (RemoteException e) {
             throw new IllegalStateException("Could not contact PowerHAL!", e);
